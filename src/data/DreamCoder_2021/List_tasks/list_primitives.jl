list_base_primitives = [
    primitive_0,
    primitive_1,
    primitive_2,
    primitive_3,
    primitive_4,
    primitive_5,
    primitive_multiplication,
    primitive_greater_than,
    primitive_is_prime,
    primitive_is_square,
    primitive_empty_list
    primitive_cons,
    primitive_car,
    primitive_cdr,
    primitive_is_empty,
    primitive_if_int,
    primitive_if_list,
    primitive_equal,
    primitive_addition,
    primitive_subtraction,
    primitive_modulus
]

"""
These are primitives provided by 1959 lisp as introduced by McCarthy
"""
list_McCarthy_primitives = [
    primitive_empty,
    primitive_cons,
    primitive_car,
    primitive_cdr,
    primitive_is_empty,
    primitive_greater_than,
    primitive_if,
    primitive_equal,
    primitive_addition,
    primitive_subtraction,
    primitive_0,
    primitive_1
]

list_function_primitives = [
    primitive_empty,
    primitive_singleton,
    primitive_range,
    primitive_append,
    primitive_map,
    primitive_reduce,
    primitive_true,
    primitive_false,
    primitive_and,
    primitive_or,
    primitive_sort,
    primitive_addition,
    primitive_multiplication,
    primitive_negation,
    primitive_modulus,
    primitive_equal,
    primitive_is_prime,
    primitive_is_square,

    primitive_sum,
    primitive_reverse,
    primitive_all,
    primitive_any,
    primitive_index,
    primitive_filter,
    primitive_slice
]

list_bootstrap_target = [
    primitive_map,
    primitive_unfold,
    primitive_range,
    primitive_index,
    primitive_fold,
    primitive_length,
    primitive_if,
    primitive_addition,
    primitive_subtraction,
    primitive_empty,
    primitive_cons,
    primitive_car,
    primitive_cdr,
    primitive_is_empty
]


