DEF run {
  WHILE(not(noMarkersPresent)) {
    IFELSE(not(markersPresent)) {
      putMarker
    }
    ELSE {
      IFELSE(not(rightIsClear)) {
        move
      }
      ELSE {
        turnRight
      }
    }
  }
}
DEF run {
  WHILE(not(rightIsClear)) {
    IFELSE(frontIsClear) {
      IF(not(frontIsClear)) {
        turnRight
      }
    }
    ELSE {
      WHILE(rightIsClear) {
        turnRight
      }
    }
  }
}
DEF run {
  REPEAT R=15 {
    IFELSE(not(leftIsClear)) {
      IFELSE(not(rightIsClear)) {
        pickMarker
      }
      ELSE {
        move
      }
    }
    ELSE {
      move
    }
  }
}
DEF run {
  turnRight move
}
DEF run {
  move
}
DEF run {
  IFELSE(not(rightIsClear)) {
    IFELSE(not(frontIsClear)) {
      WHILE(noMarkersPresent) {
        pickMarker
      }
    }
    ELSE {
      putMarker pickMarker
    }
  }
  ELSE {
    REPEAT R=5 {
      pickMarker
    }
  }
}
DEF run {
  REPEAT R=0 {
    IFELSE(noMarkersPresent) {
      WHILE(not(leftIsClear)) {
        putMarker
      }
    }
    ELSE {
      IFELSE(not(rightIsClear)) {
        turnLeft
      }
      ELSE {
        putMarker
      }
    }
  }
}
DEF run {
  WHILE(not(markersPresent)) {
    REPEAT R=3 {
      IF(not(noMarkersPresent)) {
        move
      }
    }
  }
}
DEF run {
  WHILE(not(rightIsClear)) {
    IF(not(frontIsClear)) {
      IFELSE(rightIsClear) {
        move
      }
      ELSE {
        pickMarker
      }
    }
  }
}
DEF run {
  REPEAT R=16 {
    WHILE(markersPresent) {
      REPEAT R=12 {
        turnRight
      }
    }
  }
}
